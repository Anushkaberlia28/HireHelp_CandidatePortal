# TODO

- [x] Fix blank screen after registration by implementing real registration submit flow (POST /auth/register), saving token/user to Redux/localStorage, and navigating to /dashboard.
- [ ] Run client build/lint (npm test/build) to ensure TypeScript correctness.


